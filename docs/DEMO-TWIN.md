# 售前数字孪生 · 云栖酒店 30 天经营态

> 用途：**让客户在签约前，就看到「他买了之后会获得怎样的体验和回报」**。
> 一份与真实使用毫无区别的经营数据（确定性生成、哈希链可验、全业务域覆盖），30 秒恢复，开箱即演。

## 三条命令

```bash
pnpm db:seed && pnpm demo:twin   # 种子 + 30 天经营模拟（约 1300 条五元事件）
pnpm demo:twin:snapshot          # 导出快照 → demo/twin/yunqi-30d.sql.gz（随仓库分发）
pnpm demo:twin:restore           # 售前现场：免模拟一键恢复（30 秒进入经营态）
```

## 数据集构成（2026-07-22 ~ 2026-08-20，确定性种子 mulberry32(20260821)）

| 域 | 数据 | 演示点 |
|---|---|---|
| 订单/入退 | 30 天订单确认、智能排房入住、自动结算退房（周末峰值曲线） | P2 线程/任务流 |
| 收益管理 | 每日 R1 白班调价 + R7 夜班微调（before/after/竞对依据全留痕） | P11 调价效果归因 |
| 夜班班组 | 30 个夜班：竞对采集/三轮对账/08:30 决策包（快照 v3） | P9 夜班驾驶舱 |
| 评价 | 好评自动回（auto）+ 差评 R6 必审挂起 | P4 决断队列三手势 |
| 语音前台 | 30 天电话摘要：78% FAQ 命中 / 22% 转人工 | phone-concierge 价值实证 |
| 围栏 v3 锚点 | d5 倒挂熔断（R17）/ d9 同步失败自动下架（R18）/ d12 差评 SLA 升级（R19）/ d16 布草损耗超基线（R20） | P11 价格健康页 |
| 断点闭环 | d20 身份验证失败→根因「数据缺失」→补档案字段；d24 门锁失灵→「能力边界」→应急预案更新；周断点率周报 ×4 | P10 断点看板收敛曲线 |
| FAQ 生长 | 周萃取 ×4；知识库候选 ×2（充电桩/宠物）待店长确认，来源通话可归因 | 一店一档 faq_kb |
| 审批流 | ×10：approved ×7 / pending ×2 / rejected ×1（驳回原因回流样本） | P4 三手势 + 驳回校准 |
| 内容营销 | 小红书/抖音发布 10 篇（R3 留痕） | content-marketing 看板 |

## 质量保障

- **附录 E 校验**：写入逐条过 `safeParseBusinessEvent`，验收回读完整率 100%（不达标即失败退出）；
- **哈希链**：与生产同口径（`eventHash`/canonicalJson），`pnpm db:verify-chain` 现场可验——这本身就是售前的「防篡改」演示；
- **幂等**：重复执行事件冲突丢弃、审批不重复插入，任意重跑数据集逐字节一致；
- **确定性**：固定随机种子 + 固定时间窗，两次生成的快照可二进制比对。

## 售前演示脚本建议（15 分钟）

1. **首屏（2'）**：restore 后打开 P1——「这是您店昨天的样子」；
2. **夜班战报（3'）**：P9 夜班驾驶舱，30 天每晚决策包，▲需介入只有个位数——「夜班不用人了」；
3. **技能市场（3'）**：P6，25 个官方技能逐个是可点开的资产卡，采纳率看板全是真实事件——「经验是资产，人走茶不凉」；
4. **价格健康（3'）**：P11，倒挂熔断/超售下架的检出→处置→结果三段留痕——「每年 3–8% 的渠道流失堵上了」；
5. **断点收敛（2'）**：P10，根因四分类与收敛曲线——「系统越用越聪明」；
6. **信任收口（2'）**：现场跑 `pnpm db:verify-chain`——「每一条记录都不可篡改，包括我们自己」。

## 三客群切换

快照默认是低星单体（云栖酒店 86 间）视角。民宿/无人酒店叙事用 `bundles/hotel/segment-defaults.yml` 的默认安装清单讲解即可（技能市场按客群命中对应排序）；后续可按需生成民宿版/无人酒店版孪生快照（同一生成器，换档案与权重参数）。

## 真实模型接入（B8，v2.5.2 起）

意图路由（派遣框「安排任务/提问」的第一跳）支持真实大模型：

```bash
# .env
LLM_PROVIDER=openai            # openai 兼容：deepseek/moonshot/zhipu/openai 均可
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat
```

- 配置后：dispatch 意图分类走真实模型（含提示词注入防护 + 输出白名单 + 3s 超时熔断），路由来源 `via=llm` 在任务卡留痕；
- 未配置（默认 `mock`）：确定性规则路由兜底（`via=rule`），全流程可跑（D4）；
- 模型出站强制脱敏（L6.2，任何调用路径不可绕过）。

**当前边界（诚实口径）**：意图分类可接真模型；quest 的步骤规划为确定性模板（L3 剧本，真实 LLM 规划属 dsh agent loop 融合工程）；ask 自由问答的内容合成未接线（演示时围绕已覆盖场景：调价/差评/对账/巡检/夜班/排班等，含糊指令反问澄清本身是展示点）。
